import React from 'react';

function Graph () {
    const style = {
        root: {
            background: '#f3f3f3',
            width: '100%',
            height: '333px',
        },
    };

    return (
        <div style={style.root}>
        </div>
    );
}

export default Graph;
